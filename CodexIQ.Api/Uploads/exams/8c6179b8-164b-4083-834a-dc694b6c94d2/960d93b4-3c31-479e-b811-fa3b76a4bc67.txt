main.ts genel (her zaman aynı)

app.config.ts (her zaman aynı)

app.ts boş bir şey sadece 
URL /login ise → <router-outlet> içine LoginComponent yerleşir
URL /chat ise → <router-outlet> içine ChatComponent yerleşir

app.routes.ts

tek önemli şey sitenin loadComponent kullandığı örn LoginComponent sadece login sayfasına gittiğinde indiriliyor
chat sayfası için de aynı sadece canActive var yani [authGuard] fonksiyonu çalışmadan çalışma demek

auth-guard.ts (rota koruyucusu)
AuthService ve Routerı classlarını çeker ve kullanır kontrol yapar sonra siteye yönlendirir

auth.ts (login için ana işlemler) 

login (%80 her kod için kopyala yapıştır yapılır): isim üretir örn  
email.split('@')[0] // "can.yilmaz@mail.com" → "can.yilmaz"
.replace(/[._]/g, ' ') // "can.yilmaz" → "can yilmaz"
.replace(/\b\w/g, c => c.toUpperCase()) // "can yilmaz" → "Can Yilmaz"

register : loginden tek farkı isim çıkarma işlemi yok

logout : 
this.router.navigate(['/login']); login sayfasına geri yönlendirir 
sessionStorage.removeItem('re_user'); belleği temizler








